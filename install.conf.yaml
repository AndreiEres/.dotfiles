- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.zshrc: zsh/zshrc
    ~/.config/git/config: git/config
    ~/.config/nvim/init.vim: nvim/init.vim
    ~/.tmux.conf: tmux/tmux.conf
